services:
  - type: web
    name: fw-gatekeeper
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_CONVEX_URL
        value: https://modest-bat-146.convex.cloud
